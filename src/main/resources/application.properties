# Server (Render sets the PORT variable automatically, default to 8080 locally)
server.port=${PORT:8080}

# Database (Explicit Render Internal URL)
spring.datasource.url=jdbc:postgresql://dpg-d6of9htm5p6s73d9pn00-a:5432/render1_we0a
spring.datasource.username=render1_we0a_user
spring.datasource.password=lrhX24snvyPhTxOhDNXkKoTHolewDDxr
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Initialize data (categories)
spring.sql.init.mode=always  
spring.jpa.defer-datasource-initialization=true

# JWT
app.jwt.secret=SplitMateSecretKeyForJWTTokenGenerationPleaseUseLongEnoughKey
app.jwt.expiration-ms=86400000

# CORS
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173
